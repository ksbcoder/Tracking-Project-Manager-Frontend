import { DateToRelativeTimePipe } from './date-to-relative-time.pipe';

describe('DateToRelativeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DateToRelativeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
