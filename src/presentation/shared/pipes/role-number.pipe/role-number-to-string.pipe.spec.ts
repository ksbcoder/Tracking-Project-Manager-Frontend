import { RoleNumberToStringPipe } from './role-number-to-string.pipe';

describe('RoleNumberToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleNumberToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
